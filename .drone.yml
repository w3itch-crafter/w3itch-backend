---
kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: node
    commands:
      - yarn
      - yarn build
      - yarn test:cov
---
kind: pipeline
type: ssh
name: ssh-deploy-w3itch-api-test

server:
  host: backend-api.testenv.w3itch.io
  user: w3itch
  ssh_key:
    from_secret: SSH_ID_RSA

steps:
  - name: greeting
    commands:
      - echo hello world
  - name: check-env
    commands:
      - ifconfig
      - pwd
  - name: list-easyrpg-games
    commands:
      - ls /var/easyrpg/games

  - name: build
    commands:
      - export HOME=/home/w3itch
      - export NVM_DIR="$HOME/.nvm"
      - . "$NVM_DIR/nvm.sh"
      - nvm use node
      - node -v
      - yarn
      - yarn build
      - cp * /home/w3itch/w3itch-backend-build/
      - cd /home/w3itch/w3itch-backend-build

      - export PM2_HOME=/home/w3itch/.pm2
      - pm2 status

trigger:
  branch:
    - testing
  event:
    # When a pull request is merged GitHub sends a push hook to drone
    - push

---
kind: pipeline
type: ssh
name: promotion-prod

server:
  host: api.w3itch.io
  user: w3itch
  ssh_key:
    from_secret: SSH_ID_RSA

steps:
  - name: greeting
    commands:
      - echo promotion production
  - name: check-env
    commands:
      - ifconfig
      - pwd
  - name: list-easyrpg-games
    commands:
      - ls /var/easyrpg/games
  - name: update-config

    commands:
      - echo $VERSION
      - export HOME=/home/w3itch
      - git clone -b $VERSION --single-branch ssh://git@gitea.w3itch.io:10022/w3itch-crafter/w3itch-backend-config.git
      - rm -rf /home/w3itch/w3itch-backend-config-for-rollback
      - cp -r /home/w3itch/w3itch-backend-config /home/w3itch/w3itch-backend-config-for-rollback
      - cp w3itch-backend-config/config/config.production.yaml /home/w3itch/w3itch-backend-config/config/
  - name: build
    commands:
      - yarn
      - yarn build
      - export PM2_HOME=/home/w3itch/.pm2
      - pm2 stop w3itch-backend
      - rm -rf /home/w3itch/w3itch-backend-for-rollback
      - mv /home/w3itch/w3itch-backend /home/w3itch/w3itch-backend-for-rollback
      - mkdir /home/w3itch/w3itch-backend
      - cp /home/w3itch/w3itch-backend-config/config/config.production.yaml /home/w3itch/config/
      - cp -r * /home/w3itch/w3itch-backend/
      - cp /home/w3itch/config/* /home/w3itch/w3itch-backend/config/
      - cd /home/w3itch/w3itch-backend
      - rm -rf /home/w3itch/w3itch-backend/thirdparty/games
      - ln -s /var/easyrpg/games /home/w3itch/w3itch-backend/thirdparty/games
      - rm -rf /home/w3itch/w3itch-backend/thirdparty/gencache
      - ln -s /var/easyrpg/gencache /home/w3itch/w3itch-backend/thirdparty/gencache
      - rm -rf /home/w3itch/w3itch-backend/thirdparty/downloads
      - ln -s /var/easyrpg/downloads /home/w3itch/w3itch-backend/thirdparty/downloads
      - rm -rf ./thirdparty/rtp
      - cp -r /var/easyrpg/rtp /home/w3itch/w3itch-backend/thirdparty/rtp
      - yarn typeorm:prod migration:run
      - pm2 restart w3itch-backend
trigger:
  event:
    - promote
  target:
    - production

---
kind: pipeline
type: ssh
name: rollback-prod

server:
  host: api.w3itch.io
  user: w3itch
  ssh_key:
    from_secret: SSH_ID_RSA

steps:
  - name: greeting
    commands:
      - echo rollback production
  - name: check-env
    commands:
      - ifconfig
      - pwd
  - name: list-easyrpg-games
    commands:
      - ls /var/easyrpg/games
  - name: rollback-config

    commands:
      - echo $VERSION
      - export HOME=/home/w3itch
      - rm -rf /home/w3itch/w3itch-backend-config
      - cp -r /home/w3itch/w3itch-backend-config-for-rollback /home/w3itch/w3itch-backend-config
  - name: rollback-project
    commands:
      - export PM2_HOME=/home/w3itch/.pm2
      - pm2 stop w3itch-backend
      - rm -rf /home/w3itch/w3itch-backend
      - cp -r /home/w3itch/w3itch-backend-for-rollback /home/w3itch/w3itch-backend
      - pm2 restart w3itch-backend
trigger:
  event:
    - rollback
  target:
    - production

---
kind: pipeline
type: ssh
name: ssh-deploy-w3itch-api-prod

server:
  host: api.w3itch.io
  user: w3itch
  ssh_key:
    from_secret: SSH_ID_RSA

steps:
  - name: greeting
    commands:
      - echo hello world
  - name: check-env
    commands:
      - ifconfig
      - pwd
      - node -v
  - name: list-easyrpg-games
    commands:
      - ls /var/easyrpg/games
  - name: build
    commands:
      - yarn
      - yarn build
      - export PM2_HOME=/home/w3itch/.pm2
      - pm2 stop w3itch-backend
      - rm -rf /home/w3itch/w3itch-backend-for-rollback
      - mv /home/w3itch/w3itch-backend /home/w3itch/w3itch-backend-for-rollback
      - mkdir /home/w3itch/w3itch-backend
      - cp /home/w3itch/w3itch-backend-config/config/config.production.yaml /home/w3itch/config/
      - cp -r * /home/w3itch/w3itch-backend/
      - cp /home/w3itch/config/* /home/w3itch/w3itch-backend/config/
      - cd /home/w3itch/w3itch-backend
      - rm -rf /home/w3itch/w3itch-backend/thirdparty/games
      - ln -s /var/easyrpg/games /home/w3itch/w3itch-backend/thirdparty/games
      - rm -rf /home/w3itch/w3itch-backend/thirdparty/gencache
      - ln -s /var/easyrpg/gencache /home/w3itch/w3itch-backend/thirdparty/gencache
      - rm -rf /home/w3itch/w3itch-backend/thirdparty/downloads
      - ln -s /var/easyrpg/downloads /home/w3itch/w3itch-backend/thirdparty/downloads
      - rm -rf ./thirdparty/rtp
      - cp -r /var/easyrpg/rtp /home/w3itch/w3itch-backend/thirdparty/rtp
      - yarn typeorm:prod migration:run
      - pm2 restart w3itch-backend

trigger:
  branch:
    - main
  event:
    # When a pull request is merged GitHub sends a push hook to drone
    - push
---
kind: signature
hmac: 768bdd452dcdba679a07e5d722b7e8e8d2f2c7b200b1739f44a2e4515ef020e8

...
